export const siteTitle = 'Bookmarklet Exporter';
export const siteDescription =
  'Export bookmarklets into shareable files and snippets.';

export const basePath = '/bookmarklet-exporter';

export const siteUrl =
  import.meta.env.PUBLIC_SITE_URL ?? 'https://example.github.io';

export const fullSiteUrl = `${siteUrl}${basePath}`;
